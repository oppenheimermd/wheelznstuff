
import 'package:flutter/material.dart';

class TabProfileView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("PROFILE PAGE VIEW"),
      ),
    );
  }
}